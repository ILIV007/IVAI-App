# Execution Roadmap

> **Product North Star:** IVAI is a Local-first, Backendless, BYOK **Agent Harness**. Users own and manage their provider connections, accounts, endpoints, models, capabilities, credential references, and ordered Combos. Gemini is the first streaming proof adapter only; it is not the product's fixed provider, required account, backend, or routing layer. See [Provider Harness Alignment](PROVIDER_HARNESS_ALIGNMENT.md).

## Phase 0 — Build and repository governance

**Goal:** Buildable repository, Apache-2.0 governance, focused-branch workflow, CI quality gate, and protected main branch.

**Gate:** Wrapper integrity; `assembleDebug`, `testDebugUnitTest`, and `lintDebug` pass; governance files and workflow are present; `main` is protected after the governance pull request is merged.

## Phase 1 — UI/RTL Skeleton closeout

**Goal:** Canonical navigation and chat UI state, predictable state restoration, stable test tags, dark-first design decision, and RTL/BiDi regression coverage.

**Gate:** One chat UI path, navigation tests, English/LTR shell plus mixed RTL content tests, screenshot/semantics baseline, and no real network calls.

## Phase 2 — Local data and security

**Goal:** Room, DataStore, Keystore-backed credential vault, app-private project workspace, redacted diagnostics, and versioned local export/import without secrets.

**Gate:** Migration, process-death, vault, delete-all-data, corrupted import, and redaction tests pass.

## Phase 3 — One-provider chat

**Goal:** Prove the provider-neutral adapter contract with one real Gemini chat vertical slice. This validates streaming, cancellation, persistence, and safe error handling; it does not make Gemini a fixed application provider.

**Gate:** Real stream and cancel work; history survives restart; no secret appears in logs or exports.

## Phase 4 — Provider expansion and Router

**Goal:** User-managed provider connections and accounts; OpenRouter and Custom OpenAI-compatible adapters alongside Gemini; explicit cloud provider presets; manual/discovered models; a capability-aware Combo model; sequential fallback; account health; and attempt trace.

**Current state:** Gemini, OpenRouter and generic Custom OpenAI-compatible adapters are installed. A local, non-secret cloud preset catalog supports guided setup for OpenAI, Groq, Mistral, Together, DeepSeek, Fireworks and xAI without selecting a provider, model or credential automatically. Custom OpenAI-compatible local servers may be configured only through an explicit persisted **HTTPS** loopback or RFC1918 IPv4 trust mode, user confirmation, and API-key/no-auth selection. HTTP, `.local`/mDNS discovery, scanning and cleartext opt-ins remain intentionally blocked.

**Gate:** Auth, timeout, 429, offline, cancellation, and fallback scenarios are verified without duplicate side effects. The local HTTPS trust mode has a dedicated threat model plus unit/migration/transport coverage; physical-device cancellation, timeout and mixed-network evidence remain Phase 6 hardening requirements.

## Phase 5 — Bounded Agent

**Goal:** Provide profile-selected safe read tools and at most one user-confirmed write tool with limits, trace, cancellation, and workspace isolation.

**Current state:** Calculation, current time, bounded project-file read, bounded workspace list, and bounded literal project search are local read-only tools. Workspace tools are confined to the profile project; observations remain in memory and traces retain only safe summaries. Project-file write remains the single mutation tool and requires preview plus Allow once.

**Gate:** No mutation happens without approval; every step is traceable; cancellation is clean; profile tool policy, project confinement, bounded path/size/result behavior, and non-persistence of file observations have regression coverage.

## Phase 6 — Hardening and GitHub Alpha release

**Goal:** Security review, device/RTL/accessibility matrix, recovery validation, signed APK, checksum, release notes, and GitHub release.

**Scope:** This phase does not add new providers, Agent tools, or UI screens. It closes evidence gaps only:
1. Remove global forced-LTR behavior; retain narrow LTR only for code/protocol blocks; capture Force-RTL, mixed BiDi, and TalkBack/semantics screenshots.
2. Physical-device matrix: fresh install, upgrade, restart, rotation, offline, HTTPS loopback/LAN cancellation/timeout.
3. Migration/recovery evidence: legacy upgrade, reopen, corrupted import, delete-all-data.
4. Owner-approved release signing, reproducible build, SHA-256, annotated tag, and reviewed release notes.

**Gate:** All P0 checks are green, known limitations are documented, and the owner approves the signed artifact.

**Current deterministic follow-up:** **Phase 6.1 — Launcher Monochrome Hardening** is complete in [PR #39](https://github.com/ILIV007/IVAI-App/pull/39). It adds a distinct, launcher-only monochrome vector to both Android adaptive-icon entries, removes the `MonochromeLauncherIcon` lint finding, and protects the wiring with a resource regression test. It does not alter the approved VA artwork or product UI and does not substitute for OEM launcher/device evidence, signing, checksum, or Alpha approval. See [Phase 6.1 brief](PHASE6_1_LAUNCHER_MONOCHROME_BRIEF.md) and [report](PHASE6_1_LAUNCHER_MONOCHROME_REPORT.md). **Release Candidate Preparation** now creates and verifies a local unsigned package containing deterministic build/R8/test/lint/invariant evidence for one clean commit. The package remains outside the repository and is never a signed, tagged, uploaded, public, or stable-approved artifact; field/device evidence, signing, release notes, tag, checksum for the signed artifact, and owner approval remain separate gates. See [Release Candidate Preparation](RELEASE_CANDIDATE_PREPARATION.md).

## Phase 7 — Full UI/UX Redesign

**Goal:** Complete visual and interaction redesign of all screens — Chat, Router, Agent, Provider Management, Settings, Sidebar, and onboarding — using an independent IVAI design system with a calm indigo/emerald/violet atmosphere. This phase delivers a polished, production-quality UI/UX that is consistent, accessible, and optimized for the Agent Harness workflow.

**Current state:** **7.0 — Design Foundation and Test Harness is complete.** The app has shared semantic color, spacing, shape, elevation, stroke, icon-size, motion, and layout tokens; reusable screen/header/state/target/execution primitives; light/dark Roborazzi coverage; and semantics contracts for headings, target availability, and execution announcements. **7.1 — Adaptive Navigation and Chat Foundation is complete** in [PR #27](https://github.com/ILIV007/IVAI-App/pull/27), with its acceptance screenshot coverage completed in [PR #29](https://github.com/ILIV007/IVAI-App/pull/29): compact bottom navigation plus medium/expanded rails preserve the five existing routes; the Chat-only Session Drawer contains local history search and project filtering; and Chat uses explicit target-first context, safe onboarding, and a target-aware streaming composer. **7.2 — Provider and Combo Setup Experience is complete** in [PR #30](https://github.com/ILIV007/IVAI-App/pull/30): Connections is now the unified user-managed Provider/Account/Model and Combo destination; provider creation uses Family → Endpoint/trust → Account/credential → Model/capability review; Remote HTTPS, local-device HTTPS and private-LAN HTTPS remain distinct; and Combo candidates have explicit ordering, review and final-save semantics. **7.3 — Agent Profile and Live Run Workspace is complete** in [PR #34](https://github.com/ILIV007/IVAI-App/pull/34): Agents now has a target-first local profile library, four-step Identity → explicit target → bounded tools/project → final-review editor, selected local run workspace with safe trace timeline and terminal-aware cancellation, one-time write-review sheet, post-decision UI outcome and safe recovery copy. **7.4 — Project Hub, Settings and Cross-screen Polish is complete** in [PR #36](https://github.com/ILIV007/IVAI-App/pull/36): Workspace now presents only existing local project summaries, explicit selected-project context, and deliberate routes to Chat and Agents; Settings now has concise Appearance, Connections, Privacy, and Local data sections. Workspace state cards cover loading, empty, offline, and error states without hidden recovery or fabricated activity. Settings remains limited to the existing theme toggle, Connections route, privacy commitments, and delete-local-data callback. The phase added semantic/callback and Roborazzi coverage for Workspace and Settings in dark/light states, including the all-five-destination navigation regression. The clean build, **122-test** unit suite, lint with zero errors/fatals, whitespace, secret, trust-policy, prohibited-execution, implicit-selection, global-RTL, launcher-only, protected-layer, and raw-visual-value scans passed. Review evidence is retained in [Phase 7.4 brief](PHASE7_4_PROJECT_SETTINGS_BRIEF.md) and [Phase 7.4 visual review](PHASE7_4_VISUAL_REVIEW.md). These changes are presentation-only: project file isolation, data deletion semantics, export/import behavior, permissions, Agent runtime/tools/limits/approval persistence, Provider/Router, Vault and Room/data remain unchanged. The VA artwork remains strictly Android launcher-icon artwork; product UI does not reuse the launcher mark. **7.5 — UX Research Validation and Hardening is in progress.** The deterministic preflight is green, and the no-backup policy is now explicit for Android 11-and-lower full backup plus Android 12+ cloud/device-transfer schemas; a regression test protects both rule sets. [PR #37](https://github.com/ILIV007/IVAI-App/pull/37) adds the research protocol, a de-identified evidence record, and a hardening audit. The controlled [Phase 7.5 field kit](PHASE7_5_FIELD_KIT.md), [facilitator-only scenario cards](PHASE7_5_CONTROLLED_SCENARIO_CARDS.md), and local research-build helper now prepare safe facilitator material and reproducible debug-build handoff without collecting participant data. The [field handoff audit](PHASE7_5_FIELD_HANDOFF_AUDIT_2026-08-20.md) records the package/verifier contract and explicitly leaves all participant, device, network, signing, and owner-approval gates Pending. The local package retains a commit manifest, checksums for every transferred build artifact, a quality log, and a blank local-only worksheet so a facilitator can verify provenance before a session; these materials still record no participant/device outcome and do not constitute Phase 7.5 completion. Neither preparation step claims unrun participant/device work as complete. Separate from the research protocol, the pre-Alpha deterministic remediation sequence has closed ER-02 in PR #49, ER-03 in PR #50, and ER-04 in PR #51. ER-04 preserves a non-empty visible stream partial in Room v6 with an explicit incomplete marker and never falls back after visible content. ER-05 is closed in PR #53: active Router/Gemini/OpenAI-compatible recovery paths catch only `Exception`, while fatal `Error` propagation is covered by regression tests. ER-15 is closed in PR #55: active candidate connection/account/model records are revalidated before provider open, and a missing/disabled record yields a safe failed trace instead of a collection lookup crash. ER-22/ER-25 are closed in PR #57: a checksum-valid payload with a malicious collection size is rejected before Room/files mutate. Allocation/OOM policy is intentionally unchanged without low-memory evidence. ER-24 is closed in PR #59: a present envelope with a decrypt-failing key is not reported as usable, while the envelope remains for deliberate overwrite or clear rather than silent deletion. ER-14 is closed in PR #61: R8 is enabled for the unsigned release variant, release builds produce mapping output, and CI builds the minified release variant on every PR. This does not create a signed/public Alpha artifact or substitute for physical-device validation. ER-10 is closed in PR #63: OpenAI-compatible and Gemini readers remove only one optional post-colon space from `data:` fields and preserve later leading payload whitespace; event compatibility remains intentionally unchanged. ER-13 is closed in PR #65: active gates use `checkStoredCredentialAvailability` to make explicit that the check only reads local vault availability, and a regression proves it does not open a transport; public Provider validation and live network behavior remain unchanged. ER-17 is closed in PR #67: close-count regressions reproduced the double-close in both active gates, and inner stream ownership now clears the outer reference before its single close so each successful exchange closes exactly once; protocol and network policy remain unchanged.

**Structural redesign revision (in progress):** Real-device feedback found a cropped/zoomed adaptive launcher foreground, dense compact bottom navigation alongside a separate Chat drawer, production-visible preview controls, a connection wizard that couples first model declaration to provider creation, unsafe delete-all-data confirmation, and weak light-theme parity. The approved response is a full structural rebuild of the product shell and all five destinations. **R1.1 — Launcher icon safe-zone remediation** is merged in [PR #84](https://github.com/ILIV007/IVAI-App/pull/84): it adds a padded adaptive foreground, regenerated square/round fallback assets, and a deterministic CI validator; physical-launcher evidence remains pending. **R1.2 — Semantic theme contrast foundation** is merged in [PR #85](https://github.com/ILIV007/IVAI-App/pull/85): a red/green audit identified and corrected light semantic muted-text, success and warning pair failures, and CI now checks audited Material/semantic pairs at 4.5:1 or better. Physical light-theme review remains pending. **R2 — Sidebar-first application shell** is merged in [PR #86](https://github.com/ILIV007/IVAI-App/pull/86): the compact bottom bar is removed in favor of one app-level product sidebar, local Chat history is embedded under Chat rather than in a second drawer, and production TopBar no longer exposes the theme toggle or preview-state switcher. Physical compact/medium, keyboard, Force-RTL, font-scale and TalkBack evidence remains pending. **R3 — Chat workspace rebuild** is merged in [PR #87](https://github.com/ILIV007/IVAI-App/pull/87): the composer uses the product prompt **“Do anything…”**, a visible semantic send affordance, a local draft before target selection, and a target-selection route that remains explicit rather than auto-selecting a Provider/Model. The redundant conversation-options control is removed. Deterministic Chat regression coverage is added; physical IME, light/dark send visibility, streaming stop, offline, rotation, Force-RTL, font-scale and TalkBack evidence remain pending. **R4 — Connection-first Account and Model Management** is merged in [PR #88](https://github.com/ILIV007/IVAI-App/pull/88): setup now saves a Connection plus its first Account without forcing a Model; each saved Connection exposes local Add account and Add model sheets, enabling multiple Accounts and Models beneath the same Connection. The ViewModel uses explicit foreground-only local actions with duplicate validation and vault rollback for Account creation; Model declaration requires a user-provided identifier and capabilities. The Room schema remains unchanged; existing foreign keys and the unique `(connection_id, provider_model_id)` constraint already support this relationship. No provider/model test, discovery, auto-selection, HTTP, local discovery, network change, backend, or telemetry is introduced. Focused Compose and Room regressions cover connection-only creation, independent Account/Model addition, and multiple nested records. See [R4 brief](PHASE7_R4_CONNECTION_ACCOUNT_MODEL_MANAGEMENT_BRIEF.md) and [R4 visual review](PHASE7_R4_VISUAL_REVIEW.md). Physical interaction/accessibility evidence remains pending. **R5 — Agent and Workspace Surface Rebuild** is merged in [PR #90](https://github.com/ILIV007/IVAI-App/pull/90): pending one-time project-file write approvals are now visually prioritized above profile/run actions without automatic resolution, while a selected local Agent run presents its safe trace and cancel action only when non-terminal. Workspace now distinguishes selected-project detail from the project library and exposes only persisted name, description, known local file count, and last-modified label; routes to Chat and Agents are explicit continuation actions rather than a fabricated activity feed. The Agent runtime, target selection, tool limits, approval persistence, Room schema, project-file access, provider/transport behavior, and network policy are unchanged. Focused Compose regressions and dark-state Roborazzi evidence cover the new priority and local-facts hierarchy. See [R5 brief](PHASE7_R5_AGENT_WORKSPACE_BRIEF.md) and [R5 visual review](PHASE7_R5_VISUAL_REVIEW.md). Physical interaction/accessibility evidence remains pending. **R6 — Settings Safety: Local Data Deletion Confirmation** is merged in [PR #92](https://github.com/ILIV007/IVAI-App/pull/92): selecting Delete all local data now opens a foreground confirmation rather than invoking reset immediately; the dialog lists only existing local workspace files, local database records, and encrypted stored credentials, explicitly excludes remote/provider/system/external/other-app data, and keeps Cancel as the non-destructive path. Explicit confirmation invokes the existing reset callback once; `LocalDataResetter`, Room/project/vault cleanup, error propagation, backup policy, provider/transport behavior, and network policy are unchanged. Focused Compose regression and dark-state Roborazzi evidence cover the warning hierarchy and no-early-delete contract. See [R6 brief](PHASE7_R6_SETTINGS_SAFETY_BRIEF.md) and [R6 visual review](PHASE7_R6_SETTINGS_SAFETY_VISUAL_REVIEW.md). Physical interaction/accessibility evidence remains pending. **R8 — Explicit Provider/Model Test Readiness** is merged in [PR #94](https://github.com/ILIV007/IVAI-App/pull/94): the future foreground one-shot test contract now specifies target revalidation, fresh consent, cancellation, redaction, no discovery/retry/fallback/background behavior, and CI rejects runtime test symbols before the gate opens. No Test button, transport call, probe, discovery, status mutation, telemetry, or network policy change exists in this phase. Runtime R8 remains blocked until actual Phase 7.5 participant/device evidence and the first signed Alpha release gates close. See [R8 readiness contract](PHASE7_R8_PROVIDER_MODEL_TEST_READINESS.md). The detailed internal decision record is retained with the delivery evidence. The post-R8 deterministic repository review, confirmed remediation, deferred toolchain/resource findings, and unchanged evidence gates are recorded in the [Post-R8 Engineering Audit](POST_R8_ENGINEERING_AUDIT_2026-08-19.md). **Compatibility refresh (merged in [PR #97](https://github.com/ILIV007/IVAI-App/pull/97)):** research confirmed that a broad AGP/Gradle/Kotlin/KSP migration has no single fully documented upgrade tuple in the current release streams, so it remains separate. The focused candidate refresh retains the current toolchain, Room, DataStore, Lifecycle and Compose BOM; `Core 1.19.0` was rejected because it requires `compileSdk 37`; and Activity Compose `1.13.0`, Navigation Compose `2.9.8`, and Roborazzi `1.72.0` passed clean unit/release/lint validation plus a recorded and verified visual baseline. See the [compatibility research](COMPATIBILITY_RESEARCH_2026-08-19.md), [refresh plan](COMPATIBILITY_REFRESH_PLAN_2026-08-19.md), and [visual review](COMPATIBILITY_VISUAL_REVIEW_2026-08-19.md).

**Compatibility closure update:** The previously deferred toolchain and launcher decisions are now complete through isolated, validated PRs: [#105](https://github.com/ILIV007/IVAI-App/pull/105) validated AGP 9.3.1, Gradle 9.7.1 and API 37; [#106](https://github.com/ILIV007/IVAI-App/pull/106) advanced Core to 1.19.0 and Lifecycle to 2.11.0; [#107](https://github.com/ILIV007/IVAI-App/pull/107) and [#108](https://github.com/ILIV007/IVAI-App/pull/108) updated Compose BOM 2026.08.00 and its eight local v2 test-rule imports; [#109](https://github.com/ILIV007/IVAI-App/pull/109) validated Kotlin Compose plugin 2.4.10 with KSP 2.3.11; and [#110](https://github.com/ILIV007/IVAI-App/pull/110) made the `minSdk 29` launcher policy adaptive-only. The compatibility-closure snapshot was lint-zero with 155 tests, 0 failures, 0 errors and 0 skipped; see [Compatibility Research](COMPATIBILITY_RESEARCH_2026-08-19.md). The current deterministic baseline is lint-zero with 160 tests, 0 failures, 0 errors and 0 skipped after the 2026-08-21 terminal-stream remediation, which adds provider-contract regressions that stop malformed streams after their first terminal event; see the [Terminal Stream Hardening Audit](TERMINAL_STREAM_HARDENING_AUDIT_2026-08-21.md). This deterministic compatibility closure does not replace physical device, accessibility, network, participant, signing or owner-approval evidence.

**UX rebuild execution (in progress):** The approved structural UI/UX blueprint is being executed as focused increments without mixing UI work with data-layer, provider-runtime or hardening changes. **UX-1 — Adaptive Launcher Icon Rebuild** is deterministically complete in [PR #124](https://github.com/ILIV007/IVAI-App/pull/124), merged as `82807ab4cd4bbe040abeed9820dcb486cb0de931`: the composite PNG foreground/generator was replaced with a symbol-only safe-zone VA vector, independent background and matching monochrome mask. Its focused scope and evidence are recorded in [Phase 7 UX-1](PHASE7_UX1_ADAPTIVE_ICON_REBUILD.md). It remains a launcher-only correction and does **not** close pending physical launcher/OEM/themed-icon evidence or any Phase 7.5 research/device/signing gate. **UX-2 — Design Foundation and Component Primitives** is deterministically complete in [PR #126](https://github.com/ILIV007/IVAI-App/pull/126), merged as `f404a4bdc2a46c39094729c29bc14175ea32352b`: it establishes sibling light/dark semantic tokens, a BiDi-safe type hierarchy, shared action/state/target primitives, expanded contrast regression coverage and retires unused ad-hoc state views. It does not close physical accessibility/device gates; see [UX-2 brief](PHASE7_UX2_DESIGN_FOUNDATION_BRIEF.md). UX-3 (single sidebar shell and navigation contract) begins only after this evidence update is merged.

**Current Phase 7.5 gate:** Voluntary card-sort, tree-test, moderated safety-comprehension, and heuristic findings must be recorded; compact/medium physical-device, Force-RTL, TalkBack, font-scale, offline, HTTPS loopback/private-LAN cancellation/timeout, lifecycle, signing, checksum, and release-note evidence remain pending. Phase 7 and a public Alpha release are therefore **not approved**. The Phase 7.5 research protocol itself must not add analytics, session replay, a backend, provider defaults, or unreviewed runtime/data capability. The current deterministic baseline and verified deferred gates are recorded in the [Phase 7.5 Deterministic Hardening Audit — 21 August 2026](PHASE7_5_DETERMINISTIC_HARDENING_AUDIT_2026-08-21.md). Repository documentation, GitHub governance, hygiene, and dependency-maintenance alignment are recorded separately in the [Repository Professionalization Audit — 21 August 2026](REPOSITORY_PROFESSIONALIZATION_AUDIT_2026-08-21.md); that work does not close any field/device/signing gate. The earlier [Pre-Alpha Engineering Backlog](PRE_ALPHA_ENGINEERING_BACKLOG.md), [External Review Triage](EXTERNAL_REVIEW_TRIAGE_2026-08-17.md), [Global Review — Post ER-01](GLOBAL_REVIEW_2026-08-17_POST_ER01.md), and [Pre-Field-Validation review](GLOBAL_REVIEW_2026-08-17_PRE_FIELD_VALIDATION.md) remain historical decision records. All field/device evidence remains deferred rather than complete.

**Scope:**
- Full redesign of all Compose screens and components with a unified design language
- Improved empty states, onboarding, and provider setup flows
- Consistent spacing, typography, elevation, and motion
- Dark/light theme parity with the approved independent IVAI UI palette; the launcher artwork is not reused in product screens
- Accessibility: TalkBack labels, touch targets, contrast, and BiDi text support
- Screenshot/semantics baseline for all redesigned screens
- No new provider adapters, Agent tools, or backend dependencies introduced in this phase

**Gate:** All redesigned screens pass screenshot regression, accessibility semantics, contrast checks, and the launcher-only boundary scan. No behavioral regression in provider, Agent, or data layers.

## Phase 8 — Extensible Capabilities: Skills and MCP

**Goal:** Prepare IVAI for user-owned, local Skill workflows and carefully scoped MCP capability connections without weakening Local-first, Backendless, BYOK, explicit target selection, app-private project boundaries, or one-time consent.

**Current state:** **Planning-only readiness is in progress; no runtime implementation is approved or present.** The architectural decision record is in [Future Architecture — Skills and MCP](SKILLS_MCP_FUTURE_ARCHITECTURE.md), the initial screen/consent blueprint is in [Skills and MCP — Initial UX Blueprint](SKILLS_MCP_UI_BLUEPRINT.md), the canonical state/consent/redaction/evidence vocabulary is in the [Phase 8.0 Capability Consent and Traceability Matrix](PHASE8_0_CAPABILITY_CONSENT_MATRIX.md), future assets/trust boundaries/abuse-case evidence are mapped in the [Phase 8.0 Skills and MCP Threat Model](PHASE8_0_SKILLS_MCP_THREAT_MODEL.md), and inert mock scenarios plus future static-review acceptance are in the [Phase 8.0 Prototype Review Specification](PHASE8_0_PROTOTYPE_REVIEW_SPECIFICATION.md). Skills are planned as local declarative workflow definitions; MCP server profiles are planned as user-managed, disabled-by-default capability connections. Neither is a new Provider, Agent target, automatic router, marketplace, process runner, backend, or telemetry channel. Phase 8 runtime remains blocked by the Phase 7.5 and first Alpha release prerequisites.

**Subphase sequence:**
1. **8.0 — Capability architecture decision:** terminology, trust tiers, permission vocabulary, threat model, UX acceptance matrix, and prototype only.
2. **8.1 — Local Skill registry:** app-private declarative local Skill definitions and library UI; no network, remote catalog, MCP, scheduler, or executable code.
3. **8.2 — Skill-to-Agent binding:** explicit local Skill binding through Agent final review; no automatic target/provider/capability change.
4. **8.3 — MCP profile foundation:** disabled-by-default local metadata and vault references; no connection attempt.
5. **8.4 — HTTPS foreground MCP handshake:** one user-started, exact-server Streamable HTTP compatibility/initialization flow; no discovery, background reconnect, fallback, or stdio.
6. **8.5 — Capability allowlist and safe reads:** exact resource/prompt/read-only tool grants for one Agent/Skill/project with redacted consent/trace.
7. **8.6 — One-time external mutation:** one narrowly declared mutation-like tool invocation with preflight and Allow once; no permanent grant/retry.
8. **8.7 — OAuth/authorization support:** user-mediated authorization, PKCE/issuer/resource validation, vault storage, revoke and expiry coverage.
9. **8.8 — Interoperability review:** optional Skills over MCP/extensions behind an explicit experimental flag after specification maturity and threat review.
10. **8.9 — Deferred execution threat model:** decision record for tasks, background activity, triggers, scheduling, or long-running operations before any runtime work.

**Gate:** Every subphase requires a focused threat model, deterministic tests, redaction/migration/revocation coverage, semantic and visual review, architecture scans, protected CI, and relevant physical-device evidence. A Skill never grants authority by itself; an MCP server declaration is untrusted; no user data or tool is exposed/invoked without explicit, user-visible consent. Current Phase 7.5 research/device evidence and first Alpha signing/release gates remain prerequisites before Phase 8 implementation begins.

## Deferred after Alpha

Cloud-drive backup, local-model inference, voice, Termux/Shell, Shizuku, Accessibility automation, race/council routing, smart routing, multi-device sync, public app-store distribution, MCP stdio/process hosting, remote Skill marketplaces, dynamic code loading, MCP Apps, background Agent/MCP execution, webhooks, polling, scheduling, and persistent reconnect require separate decisions and threat models. HTTP, `.local`/mDNS discovery, LAN scans, automatic endpoint discovery, wildcard server trust, global tool grants, automatic OAuth scope escalation, and permanent external mutation approval remain rejected unless a future named phase reverses the decision with explicit evidence.
